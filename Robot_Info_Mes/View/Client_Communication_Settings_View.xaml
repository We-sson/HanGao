<UserControl
    x:Class="Robot_Info_Mes.View.Client_Communication_Settings_View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Enum_Converter="clr-namespace:Robot_Info_Mes.View"
    xmlns:Robot="clr-namespace:Roboto_Socket_Library.Model;assembly=Roboto_Socket_Library"
    xmlns:ViewModel="clr-namespace:Robot_Info_Mes.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=ViewModel:Robot_Info_VM,
                                     IsDesignTimeCreatable=False}"
    mc:Ignorable="d">



    <Grid Width=" 500">
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>

        <Border
            Panel.ZIndex="1"
            Background="{DynamicResource 全局_按钮_白颜色}"
            BorderBrush="{DynamicResource 全局_背景_蓝颜色}"
            BorderThickness="3,3,3,0"
            CornerRadius="12 12 0 0"
            Effect="{DynamicResource 全局背景阴影}">
            <Grid Margin="22,0,18,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{DynamicResource 全局_文字_中文字体}"
                        FontSize="19"
                        FontWeight="SemiBold"
                        Foreground="#344F6D"
                        Text="通信设置" />
                    <TextBlock
                        Margin="18,3,0,0"
                        VerticalAlignment="Center"
                        FontFamily="{DynamicResource 全局_文字_中文字体}"
                        FontSize="13"
                        Foreground="#8793A2"
                        Text="机器人协议、看板连接和本机服务参数" />
                </StackPanel>

            </Grid>
        </Border>

        <!--  参数顺序和排版保持原 WPF 纵向单列格式。  -->

        <Border
            Grid.Row="1"
            BorderBrush="{DynamicResource 全局_背景_蓝颜色}"
            BorderThickness="3,0,3,0">

            <ScrollViewer
                Background="{DynamicResource 全局_背景_灰白颜色}"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">


                <StackPanel Margin="10" Orientation="Vertical">



                    <Grid Height=" 35" Margin="0,8,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="85" />
                            <ColumnDefinition />
                            <ColumnDefinition Width=" auto" />
                            <ColumnDefinition Width=" 30" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column=" 0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="机器类型" />
                        <ComboBox
                            Grid.Column=" 1"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            FontSize="16"
                            IsDropDownOpen="False"
                            IsSynchronizedWithCurrentItem="True"
                            ItemsSource="{Enum_Converter:EnumBindingSource Robot:Socket_Robot_Protocols_Enum}"
                            SelectedValue="{Binding File_Int_Parameters.Mes_Run_Parameters.Socket_Robot_Model}">
                            <ComboBox.Style>
                                <Style BasedOn="{StaticResource Data_ComboBoxStyle}" TargetType="ComboBox">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>

                                        <DataTrigger Binding="{Binding Robot_Info_Parameters.Sever_IsRuning}" Value="True">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        Padding="0,4,0,4"
                                        FontFamily="{DynamicResource 全局_文字_数字字体}"
                                        FontSize="14"
                                        Foreground="{DynamicResource 全局_按钮_黑颜色}"
                                        Text="{Binding}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="Wrap" />


                                </DataTemplate>
                            </ComboBox.ItemTemplate>

                        </ComboBox>


                    </Grid>



                    <Grid Height="35" Margin="0,8,0,8">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition />
                            <ColumnDefinition Width=" auto" />
                            <ColumnDefinition Width=" 30" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="保存时间" />
                        <Slider
                            x:Name="Polling_Time_UI"
                            Grid.Column=" 1"
                            Margin="5"
                            VerticalAlignment="Center"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            IsSnapToTickEnabled="True"
                            Maximum="10"
                            Minimum="0.1"
                            SmallChange="0.1"
                            Style="{DynamicResource Vision_SliderStyle}"
                            TickFrequency="0.1"
                            Value="{Binding File_Int_Parameters.Mes_Run_Parameters.File_Save_Cycle_Time}" />

                        <TextBox
                            Grid.Column=" 2"
                            Width=" 200"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Background="Transparent"
                            BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                            BorderThickness="2"
                            FontFamily="{DynamicResource 全局_文字_数字字体}"
                            FontSize="18"
                            Style="{DynamicResource Vision_TextBoxStyle}"
                            Text="{Binding ElementName=Polling_Time_UI, Path=Value, Mode=TwoWay, StringFormat={}{0:F1}}" />

                        <TextBlock
                            Grid.Column=" 3"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="秒" />
                    </Grid>


                    <Grid Height="35" Margin="0,8,0,8">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition />
                            <ColumnDefinition Width=" auto" />
                            <ColumnDefinition Width=" 30                                                           " />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="看板更新" />
                        <Slider
                            x:Name="Sever_Cycle_Update_Time_UI"
                            Grid.Column=" 1"
                            Margin="5"
                            VerticalAlignment="Center"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            IsSnapToTickEnabled="True"
                            Maximum="10"
                            Minimum="0.1"
                            SmallChange="0.1"
                            Style="{DynamicResource Vision_SliderStyle}"
                            TickFrequency="0.1"
                            Value="{Binding File_Int_Parameters.Mes_Run_Parameters.Sever_Cycle_Update_Time}" />

                        <TextBox
                            Grid.Column=" 2"
                            Width=" 200"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Background="Transparent"
                            BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                            BorderThickness="2"
                            FontFamily="{DynamicResource 全局_文字_数字字体}"
                            FontSize="18"
                            Style="{DynamicResource Vision_TextBoxStyle}"
                            Text="{Binding ElementName=Sever_Cycle_Update_Time_UI, Path=Value, Mode=TwoWay, StringFormat={}{0:F1}}" />

                        <TextBlock
                            Grid.Column=" 3"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="秒" />
                    </Grid>

                    <Grid Height="35" Margin="0,8,0,8">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition />
                            <ColumnDefinition Width=" auto" />
                            <ColumnDefinition Width=" 30" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="看板IP" />


                        <TextBox
                            Grid.Column=" 2"
                            Width=" 200"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Background="Transparent"
                            BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                            BorderThickness="2"
                            FontFamily="{DynamicResource 全局_文字_数字字体}"
                            FontSize="18"
                            Style="{DynamicResource Vision_TextBoxStyle}"
                            Text="{Binding File_Int_Parameters.Mes_Run_Parameters.Sever_Mes_Info_IP, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>

                    <Grid Height="35" Margin="0,8,0,8">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition />
                            <ColumnDefinition Width=" auto" />
                            <ColumnDefinition Width=" 30" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="看板端口" />


                        <TextBox
                            Grid.Column=" 2"
                            Width=" 200"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Background="Transparent"
                            BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                            BorderThickness="2"
                            FontFamily="{DynamicResource 全局_文字_数字字体}"
                            FontSize="18"
                            Style="{DynamicResource Vision_TextBoxStyle}"
                            Text="{Binding File_Int_Parameters.Mes_Run_Parameters.Sever_Mes_Info_Port, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                    <Grid Height="35" Margin="0,8,0,8">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition />
                            <ColumnDefinition Width=" auto" />
                            <ColumnDefinition Width=" 30" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="看板超时" />
                        <Slider
                            x:Name="Mes_Server_Info_Rece_Time_UI"
                            Grid.Column=" 1"
                            Margin="5"
                            VerticalAlignment="Center"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            IsSnapToTickEnabled="True"
                            Maximum="10"
                            Minimum="0.1"
                            SmallChange="0.1"
                            Style="{DynamicResource Vision_SliderStyle}"
                            TickFrequency="0.1"
                            Value="{Binding File_Int_Parameters.Mes_Run_Parameters.Mes_Server_Info_Rece_Time}" />

                        <TextBox
                            Grid.Column=" 2"
                            Width=" 200"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Background="Transparent"
                            BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                            BorderThickness="2"
                            FontFamily="{DynamicResource 全局_文字_数字字体}"
                            FontSize="18"
                            Style="{DynamicResource Vision_TextBoxStyle}"
                            Text="{Binding ElementName=Mes_Server_Info_Rece_Time_UI, Path=Value, Mode=TwoWay, StringFormat={}{0:F1}}" />

                        <TextBlock
                            Grid.Column=" 3"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="秒" />
                    </Grid>


                    <Grid Height=" 35" Margin="0,8,0,8">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition />
                            <ColumnDefinition Width=" 30" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column=" 0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="本机IP" />
                        <ComboBox
                            Grid.Column=" 1"
                            HorizontalContentAlignment="Left"
                            VerticalContentAlignment="Center"
                            BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                            BorderThickness="2"
                            IsDropDownOpen="False"
                            IsSynchronizedWithCurrentItem="True"
                            ItemsSource="{Binding Robot_Info_Parameters.Local_IP_UI}"
                            SelectedValue="{}"
                            Style="{DynamicResource Data_ComboBoxStyle}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        Padding="0,4,0,4"
                                        FontFamily="{DynamicResource 全局_文字_数字字体}"
                                        FontSize="16"
                                        Foreground="{DynamicResource 全局_按钮_黑颜色}"
                                        Text="{Binding}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="Wrap" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>

                            <i:Interaction.Triggers>

                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction
                                        Command="{Binding}"
                                        CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}, Path=EventArgsConverter}"
                                        PassEventArgsToCommand="True" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>


                        </ComboBox>
                    </Grid>
                    <Grid Height="35" Margin="0,8,0,8">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition />
                            <ColumnDefinition Width=" auto" />
                            <ColumnDefinition Width=" 30" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="{DynamicResource 全局_文字_中文字体}"
                            FontSize="16"
                            Foreground="{DynamicResource 全局_按钮_黑颜色}"
                            Text="服务端口" />


                        <TextBox
                            Grid.Column=" 2"
                            Width=" 200"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Background="Transparent"
                            BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                            BorderThickness="2"
                            FontFamily="{DynamicResource 全局_文字_数字字体}"
                            FontSize="18"
                            Style="{DynamicResource Vision_TextBoxStyle}"
                            Text="{Binding File_Int_Parameters.Mes_Run_Parameters.Sever_Socket_Port, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>




                </StackPanel>


                <!--<StackPanel Margin="22,12,22,10">
                                <Grid Height="46" Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource PopupParameterLabelStyle}" Text="机器类型" />
                                    <ComboBox
                                        Grid.Column="1"
                                        Height="38"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        ItemsSource="{Enum_Converter:EnumBindingSource Robot:Socket_Robot_Protocols_Enum}"
                                        SelectedValue="{Binding File_Int_Parameters.Mes_Run_Parameters.Socket_Robot_Model}"
                                        Style="{DynamicResource Data_ComboBoxStyle}" />
                                </Grid>

                                <Grid Height="46" Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource PopupParameterLabelStyle}" Text="保存时间" />
                                    <Slider
                                        x:Name="Popup_File_Save_Cycle_Time"
                                        Grid.Column="1"
                                        Margin="5"
                                        VerticalAlignment="Center"
                                        IsSnapToTickEnabled="True"
                                        Maximum="10"
                                        Minimum="0.1"
                                        SmallChange="0.1"
                                        Style="{DynamicResource Vision_SliderStyle}"
                                        TickFrequency="0.1"
                                        Value="{Binding File_Int_Parameters.Mes_Run_Parameters.File_Save_Cycle_Time}" />
                                    <TextBox
                                        Grid.Column="3"
                                        Height="38"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                                        BorderThickness="2"
                                        FontFamily="{DynamicResource 全局_文字_数字字体}"
                                        FontSize="18"
                                        Style="{DynamicResource Vision_TextBoxStyle}"
                                        Text="{Binding ElementName=Popup_File_Save_Cycle_Time, Path=Value, Mode=TwoWay, StringFormat={}{0:F1}}" />
                                    <TextBlock
                                        Grid.Column="4"
                                        HorizontalAlignment="Center"
                                        Style="{StaticResource PopupParameterLabelStyle}"
                                        Text="秒" />
                                </Grid>

                                <Grid Height="46" Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource PopupParameterLabelStyle}" Text="看板更新" />
                                    <Slider
                                        x:Name="Popup_Sever_Cycle_Update_Time"
                                        Grid.Column="1"
                                        Margin="5"
                                        VerticalAlignment="Center"
                                        IsSnapToTickEnabled="True"
                                        Maximum="10"
                                        Minimum="0.1"
                                        SmallChange="0.1"
                                        Style="{DynamicResource Vision_SliderStyle}"
                                        TickFrequency="0.1"
                                        Value="{Binding File_Int_Parameters.Mes_Run_Parameters.Sever_Cycle_Update_Time}" />
                                    <TextBox
                                        Grid.Column="3"
                                        Height="38"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                                        BorderThickness="2"
                                        FontFamily="{DynamicResource 全局_文字_数字字体}"
                                        FontSize="18"
                                        Style="{DynamicResource Vision_TextBoxStyle}"
                                        Text="{Binding ElementName=Popup_Sever_Cycle_Update_Time, Path=Value, Mode=TwoWay, StringFormat={}{0:F1}}" />
                                    <TextBlock
                                        Grid.Column="4"
                                        HorizontalAlignment="Center"
                                        Style="{StaticResource PopupParameterLabelStyle}"
                                        Text="秒" />
                                </Grid>

                                <Grid Height="46" Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="200" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource PopupParameterLabelStyle}" Text="看板IP" />
                                    <TextBox
                                        Grid.Column="2"
                                        Height="38"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                                        BorderThickness="2"
                                        FontFamily="{DynamicResource 全局_文字_数字字体}"
                                        FontSize="18"
                                        Style="{DynamicResource Vision_TextBoxStyle}"
                                        Text="{Binding File_Int_Parameters.Mes_Run_Parameters.Sever_Mes_Info_IP, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </Grid>

                                <Grid Height="46" Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="200" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource PopupParameterLabelStyle}" Text="看板端口" />
                                    <TextBox
                                        Grid.Column="2"
                                        Height="38"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                                        BorderThickness="2"
                                        FontFamily="{DynamicResource 全局_文字_数字字体}"
                                        FontSize="18"
                                        Style="{DynamicResource Vision_TextBoxStyle}"
                                        Text="{Binding File_Int_Parameters.Mes_Run_Parameters.Sever_Mes_Info_Port, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </Grid>

                                <Grid Height="46" Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource PopupParameterLabelStyle}" Text="看板超时" />
                                    <Slider
                                        x:Name="Popup_Mes_Server_Info_Rece_Time"
                                        Grid.Column="1"
                                        Margin="5"
                                        VerticalAlignment="Center"
                                        IsSnapToTickEnabled="True"
                                        Maximum="10"
                                        Minimum="0.1"
                                        SmallChange="0.1"
                                        Style="{DynamicResource Vision_SliderStyle}"
                                        TickFrequency="0.1"
                                        Value="{Binding File_Int_Parameters.Mes_Run_Parameters.Mes_Server_Info_Rece_Time}" />
                                    <TextBox
                                        Grid.Column="3"
                                        Height="38"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                                        BorderThickness="2"
                                        FontFamily="{DynamicResource 全局_文字_数字字体}"
                                        FontSize="18"
                                        Style="{DynamicResource Vision_TextBoxStyle}"
                                        Text="{Binding ElementName=Popup_Mes_Server_Info_Rece_Time, Path=Value, Mode=TwoWay, StringFormat={}{0:F1}}" />
                                    <TextBlock
                                        Grid.Column="4"
                                        HorizontalAlignment="Center"
                                        Style="{StaticResource PopupParameterLabelStyle}"
                                        Text="秒" />
                                </Grid>

                                <Grid Height="46" Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource PopupParameterLabelStyle}" Text="本机IP" />
                                    <ComboBox
                                        Grid.Column="1"
                                        Height="38"
                                        HorizontalContentAlignment="Left"
                                        VerticalContentAlignment="Center"
                                        IsSynchronizedWithCurrentItem="True"
                                        ItemsSource="{Binding Robot_Info_Parameters.Local_IP_UI}"
                                        Style="{DynamicResource Data_ComboBoxStyle}" />
                                </Grid>

                                <Grid Height="46" Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="200" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource PopupParameterLabelStyle}" Text="服务端口" />
                                    <TextBox
                                        Grid.Column="2"
                                        Height="38"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource 全局_按钮_黑颜色}"
                                        BorderThickness="2"
                                        FontFamily="{DynamicResource 全局_文字_数字字体}"
                                        FontSize="18"
                                        Style="{DynamicResource Vision_TextBoxStyle}"
                                        Text="{Binding File_Int_Parameters.Mes_Run_Parameters.Sever_Socket_Port, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </Grid>
                            </StackPanel>-->
            </ScrollViewer>
        </Border>

        <Border
            Grid.Row="2"
            Background="{DynamicResource 全局_按钮_白颜色}"
            BorderBrush="{DynamicResource 全局_背景_蓝颜色}"
            BorderThickness="3,0,3,3"
            CornerRadius=" 0 0 12 12">
            <Grid Margin="22,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <CheckBox
                    Width="100"
                    Height="36"
                    d:IsChecked="True"
                    Background="{DynamicResource 全局_按钮_蓝颜色}"
                    IsChecked="{Binding Robot_Info_Parameters.Sever_IsRuning}"
                    Style="{DynamicResource CheckBoxStyle1}">
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{DynamicResource 全局_文字_中文字体}"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource 全局_按钮_白颜色}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="开启服务" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Robot_Info_Parameters.Sever_IsRuning}" Value="True">
                                        <Setter Property="Text" Value="关闭服务" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction
                                Command="{Binding Server_End_Comm}"
                                CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}, Path=EventArgsConverter}"
                                PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </CheckBox>

                <TextBlock
                    Grid.Column="1"
                    Margin="18,0"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Foreground="#8793A2"
                    Text="参数调整后点击保存，重启软件后生效" />

                <Button
                    Grid.Column="2"
                    Width="102"
                    Height="36"
                    Background="{DynamicResource 全局_按钮_蓝颜色}"
                    Style="{DynamicResource Halcon_Window_Button}">
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{DynamicResource 全局_文字_中文字体}"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Text="保存参数" />
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction
                                Command="{Binding Save_Date_File_Comm}"
                                CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}, Path=EventArgsConverter}"
                                PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </Grid>
        </Border>
    </Grid>

</UserControl>

