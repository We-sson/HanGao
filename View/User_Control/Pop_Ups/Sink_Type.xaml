<UserControl x:Class="HanGao.View.User_Control.Pop_Ups.Sink_Type"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HanGao.View.User_Control.Pop_Ups"
             Width="250" Height=" 280"
             DataContext="{Binding UC_Sink_Type_VM, Source={StaticResource Locator}}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                CornerRadius=" 5"
                Background="{Binding Background, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Border}}}"
                BorderThickness="5" BorderBrush="Gray">



            <TextBlock Text="{Binding Sink_Ico}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontFamily="{Binding Font_Family}" />

        </Border>


        <TextBlock Grid.Row="1"
                   Text="{Binding Check_State}"
                   Margin="5" HorizontalAlignment="Center"
                   FontSize="20" />


    </Grid>
</UserControl>
